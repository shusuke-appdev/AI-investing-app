# src/ui

This package is a frozen archive of the pre-Reflex Streamlit UI modules.

The active UI is `frontend/`. New product behavior, UX fixes, and state changes
should be implemented in the Reflex code path unless a task explicitly targets
legacy migration reference code.
